import ComputerPlayer from '../ComputerPlayer/ComputerPlayer';

const npc = new ComputerPlayer();

test('Test string is returned', () => {
    expect(npc.selectOption).toBe();
    // expect(true).toBeTruthy();
})